# oura-toolkit namespace root. HAND-WRITTEN (never touched by `just gen`): submodules are
# `oura_toolkit.api` (generated client — do not hand-edit) and `oura_toolkit.auth`
# (hand-written auth companion: token store, refresh with rotation, spec-pinned metadata).
