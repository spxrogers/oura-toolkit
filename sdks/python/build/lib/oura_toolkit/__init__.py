# oura-toolkit namespace root. HAND-WRITTEN (never touched by `just gen`): submodules are
# `oura_toolkit.api` (generated client — do not hand-edit) and, when it lands (#15),
# `oura_toolkit.auth` (hand-written companion).
