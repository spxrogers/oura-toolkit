//! oura-cli — the Oura Ring toolkit CLI (auth flows, data commands, and `--mcp`).
//!
//! Skeleton only. Interactive auth (`auth setup` / `auth login`) lands in #8, data commands
//! in #9, and the `--mcp` STDIO server in #10.

fn main() {
    eprintln!(
        "oura-cli: not yet implemented — see https://github.com/spxrogers/oura-toolkit/issues/8"
    );
}
